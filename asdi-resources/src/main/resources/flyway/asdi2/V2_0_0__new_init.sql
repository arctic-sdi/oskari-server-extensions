-- NOTE! This will not initialize content to empty database. An existing database dump is required to setup the app.
-- There are some init files in this maven module that would result in a close approximation but its not worth to spend
-- effort creating the empty db setup before we actually need it so this is a placeholder for asdi2 module of migrations.
SELECT now();
